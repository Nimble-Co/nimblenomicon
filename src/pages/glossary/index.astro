---
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
import { compareGlossaryRowsForListing, glossary } from '../../models/glossary';

const rows = [...glossary].sort(compareGlossaryRowsForListing);
---

<StarlightPage
	frontmatter={{
		title: 'Glossary',
		description:
			'Alphabetical glossary of terms with links to each definition.',
		tableOfContents: false,
		prev: false,
		next: false,
	}}
	hasSidebar={false}
>
	<div class="sl-markdown-content not-content">
		<table class="glossary-index__table">
			<thead>
				<tr>
					<th scope="col">Term</th>
				</tr>
			</thead>
			<tbody>
				{
					rows.map((row) => (
						<tr>
							<th scope="row">
								<a href={`./${row.id}/`}>{row.name}</a>
							</th>
						</tr>
					))
				}
			</tbody>
		</table>
	</div>
</StarlightPage>

<style>
	.glossary-index__table {
		width: 100%;
		border-collapse: collapse;
	}
	.glossary-index__table th,
	.glossary-index__table td {
		text-align: start;
		padding: 0.35rem 0.75rem 0.35rem 0;
		border-bottom: 1px solid var(--sl-color-hairline);
		vertical-align: top;
	}
	.glossary-index__table thead th {
		font-weight: 600;
	}
</style>
