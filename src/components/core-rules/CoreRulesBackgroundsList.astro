---
import { backgrounds } from '../../models/backgrounds';
---

{
	backgrounds.map((segment) => (
		<div id={segment.id}>
			<a href={`/backgrounds/${segment.id}/`}>
				<strong>{segment.name}.</strong>
			</a>
			{' ' + segment.description}
		</div>
	))
}
