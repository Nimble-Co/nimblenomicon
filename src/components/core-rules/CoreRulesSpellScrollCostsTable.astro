---
import CoreRulesTableThead from './CoreRulesTableThead.astro';
import { spellScrollCosts } from '../../models/spell-scroll-costs';
---

<table>
	<CoreRulesTableThead labels={['SPELL', 'TYPICAL PRICE']} />
	<tbody>
		{
			spellScrollCosts.map((row) => (
				<tr>
					<td>{row.name}</td>
					<td>{row.cost}</td>
				</tr>
			))
		}
	</tbody>
</table>
