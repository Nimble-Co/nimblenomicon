---
import { listLabel } from '../../lib/list-label';
import { downtimeActivities } from '../../models/downtime-activities';
---

<ul>
	{
		downtimeActivities.map((activity) => (
			<li>
				<strong>{listLabel(activity.name)}</strong> {activity.description}
			</li>
		))
	}
</ul>
