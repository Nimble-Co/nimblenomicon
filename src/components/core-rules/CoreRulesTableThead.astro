---
interface Props {
	labels: string[];
}

const { labels } = Astro.props;
---

<thead>
	<tr>
		{
			labels.map((label) => (
				<th>
					<strong>
						<em>{label}</em>
					</strong>
				</th>
			))
		}
	</tr>
</thead>
