---
import { listLabel } from '../../lib/list-label';
import { saveTypes } from '../../models/save-types';
---

<ul>
	{
		saveTypes.map((saveType) => (
			<li>
				<strong>{listLabel(`${saveType.name} Save`)}</strong>{' '}
				{saveType.description}
			</li>
		))
	}
</ul>
