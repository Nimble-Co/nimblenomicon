---
import { saveTypes } from '../../models/save-types';
---

<ul>
	{
		saveTypes.map((saveType) => (
			<li>
				<strong>{`${saveType.name} Save`}.</strong>{' '}
				{saveType.description}
			</li>
		))
	}
</ul>
