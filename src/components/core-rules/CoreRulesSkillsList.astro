---
import { renderMarkdown } from '../../lib/render-markdown';
import { skills } from '../../models/skills';
---

{
	skills.map(async (skill) => (
		<div
			set:html={await renderMarkdown(
				`**${skill.name}** (${skill.stat}) ${skill.description}`,
			)}
		/>
	))
}
