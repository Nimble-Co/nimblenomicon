---
import { renderMarkdown } from '../../lib/render-markdown';
import { dcExamples } from '../../models/dc-examples';
---

<ul>
	{
		dcExamples.map(async (row) => (
			<li
				set:html={await renderMarkdown(`**${row.name}:** ${row.description}`)}
			/>
		))
	}
</ul>
