---
import { listLabel } from '../../lib/list-label';
import { statArrays } from '../../models/stat-arrays';
---

<ul>
	{
		statArrays.map((arr) => (
			<li>
				<strong>{listLabel(arr.name, ':')}</strong> {arr.description}
			</li>
		))
	}
</ul>
