---
import { listLabel } from '../../lib/list-label';
import {
glossary
} from '../../models/glossary';
---

{
	glossary.map((entry) => (
		<p id={entry.id}>
			<a href={`/glossary/${entry.id}/`}>
				<strong>{listLabel(entry.name)}</strong>
			</a>
			{' ' + entry.description}
		</p>
	))
}
