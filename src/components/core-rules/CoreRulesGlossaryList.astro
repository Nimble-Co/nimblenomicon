---
import {
glossary
} from '../../models/glossary';
---

{
	glossary.map((entry) => (
		<p id={entry.id}>
			<a href={`/glossary/${entry.id}/`}>
				<strong>{entry.name}.</strong>
			</a>
			{' ' + entry.description}
		</p>
	))
}
